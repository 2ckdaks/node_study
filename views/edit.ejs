<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link href="/main.css" rel="stylesheet" />
  </head>
  <body class="grey-bg">
    <%- include('nav.ejs') %>
    <!-- rest api를위해 methodoverride를 사용하여 put경로로 전송 -->
    <form class="form-box" action="/edit?_method=PUT" method="post">
      <h4>글쓰기</h4>
      <input name="_id" value="<%= result._id %>" />
      <input name="title" value="<%= result.title %>" />
      <input name="content" value="<%= result.content %>" />
      <button type="submit">전송</button>
    </form>
  </body>
</html>
